# 剧本全要素可视化前端工程 — 执行计划

## 目标
构建一个「剧本可视化分析/创作辅助」Web 应用，将剧本从皮到骨到肉完全可视化：
- 人物（角色画像、出场统计、关系网）
- 道具（贯穿道具、出现场景、关联人物）
- 场景（场景地图/时间线、地点关联）
- 关系拓扑（人物-道具-场景 三元关联图）
- 情绪曲线（逐场情绪波动、人物情绪弧）
- 剧本评估（结构诊断：节奏、张力、角色均衡度）
- 双向用途：① 评估剧本 ② 辅助 agent 建立拓扑关系做创作设计

## 数据策略
无用户上传剧本 → 内置一个示例剧本数据（虚构短片剧本《归途》约10场，含完整人物/道具/场景/情绪标注），数据以结构化 JSON（TypeScript 类型）承载，架构上预留"导入剧本 JSON / 由 agent 生成"的扩展口。

## 阶段设计

### Stage 1 — 编排与设计
- 加载技能：`vibecoding-webapp-swarm`（设计先行的 React 工作流）
- 由主 agent 按技能规范产出设计方向 + 数据模型，再分派子代理
- 技术栈：React + TypeScript + Tailwind + shadcn/ui
- 可视化库：Recharts（情绪曲线/统计图）+ 自研 SVG/Canvas 关系拓扑图（力导向或环形布局）

### Stage 2 — 页面构建（子代理并行/接力）
核心视图：
1. 总览 Dashboard：剧本元信息 + 关键指标 + 迷你情绪曲线
2. 人物页：角色卡、出场热力、人物关系网、单人情绪弧
3. 道具页：道具时间线、道具-场景-人物关联
4. 场景页：场记表、场景地图、时间/地点/天气维度
5. 拓扑关系图：人物-道具-场景 三元力导向图（可交互、可筛选、可点选节点看详情）
6. 情绪曲线页：整场情绪波动曲线（Recharts）、人物情绪叠加对比、情绪-事件标注
7. 剧本评估页：结构诊断雷达图、节奏分析（张力度分布）、角色戏份均衡、三幕结构标注

### Stage 3 — 集成与验证
- 集成所有视图，统一设计系统
- 构建验证（build 必须通过）
- 交付前调用 website_version_manager 保存版本

## 交付物
- 完整 React 前端工程源码
- 网站版本（build_version 保存，供预览）
